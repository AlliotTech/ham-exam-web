import type { NextConfig } from "next";

// Empty config file removed would also work; keeping a minimal explicit config helps future edits.
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;


